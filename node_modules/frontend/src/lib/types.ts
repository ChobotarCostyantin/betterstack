export interface Software {
    id: number;
    name: string;
    slug: string;
    categoryIds: number[];
    shortDescription: string;
    logoUrl?: string;
    developer?: string;
    fullDescription?: string;
    websiteUrl?: string;
    githubUrl?: string;
    screenshots?: string[];
    features: Record<number, boolean | number | string>;
}
export interface Category {
    id: number;
    name: string;
    slug: string;
}

export interface Criterion {
    id: number;
    name: string;
    type: string;
    weight: number;
}
